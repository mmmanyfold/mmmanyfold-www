(ns mmm.db)

(def default-db
  {:active-view nil
   :view-titles {:title    "mmmanyfold"
                 :subtitle "web + app dev studio"}
   :profiles    {:davm {:github   nil
                        :twitter  nil
                        :name     nil
                        :projects []}
                 :elle {:projects []}}})

